name: syntax
on: push
jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.7
      - run: echo Bonciu Alin
      - run: python src/main.py 3 6
      - run: if [ $(cmd) == "9" ];
      echo E ok
      else echo Not good
              fi
